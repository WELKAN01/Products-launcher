# 🚀 Guía de Despliegue con Docker Compose y Git Submodules

Esta guía describe paso a paso cómo **desplegar el aplicativo usando Docker Compose** y cómo **trabajar correctamente con Git Submodules**, de forma clara y ordenada.

---

## 🐳 Despliegue del aplicativo con Docker Compose

### 1️⃣ Crear y configurar el archivo `.env`

- Copia el archivo de ejemplo:

```bash
cp .env.example .env
```

- Ajusta las variables necesarias según tu entorno.

---

### 2️⃣ Construir y levantar los servicios

```bash
docker-compose up -d --build
```

Esto:

- Construye las imágenes
- Levanta los contenedores en segundo plano (`-d`)

---

### 3️⃣ Verificar que la aplicación está corriendo

Abre tu navegador y accede a:

👉 **http\://localhost:3000**

---

### 4️⃣ Logs y administración básica

- 📜 Ver logs:

  ```bash
  docker-compose logs
  ```

- ⛔ Detener servicios:

  ```bash
  docker-compose down
  ```

- 🔄 Reiniciar servicios:

  ```bash
  docker-compose restart
  ```

---

### 5️⃣ Eliminar y levantar nuevamente los servicios

- Eliminar contenedores, volúmenes y redes:

```bash
docker-compose down -v
```

- Volver a construir y levantar:

```bash
docker-compose up -d --build
```

---

## 🔗 Pasos para crear y usar Git Submodules

### 1️⃣ Crear un nuevo repositorio en GitHub

Crea el repositorio que será usado como sub-módulo.

---

### 2️⃣ Clonar el repositorio principal

```bash
git clone <url-repositorio-principal>
```

---

### 3️⃣ Añadir el sub-módulo

> ⚠️ La carpeta destino **no debe existir** previamente.

```bash
git submodule add <repository_url> <directory_name>
```

---

### 4️⃣ Guardar los cambios

```bash
git add .
git commit -m "Add submodule"
git push
```

---

### 5️⃣ Inicializar y actualizar sub-módulos

Cuando alguien clona el repositorio por primera vez:

```bash
git submodule update --init --recursive
```

---

### 6️⃣ Actualizar las referencias del sub-módulo

```bash
git submodule update --remote
```

---

## ⚠️ Importante

Si trabajas en un repositorio que **contiene sub-módulos**:

1. **Primero** haz `commit` y `push` en el sub-módulo
2. **Después** haz `commit` y `push` en el repositorio principal

❗ Si se hace al revés, se perderán las referencias y se generarán conflictos.

---

## ✅ Resumen rápido de comandos Docker Compose

| Situación                                | Comando recomendado                                    |
| ---------------------------------------- | ------------------------------------------------------ |
| Cambié solo código (`src`)               | `docker compose up`                                    |
| Cambié dependencias o Dockerfile (uno)   | `docker compose build <servicio> && docker compose up` |
| Cambié dependencias o Dockerfile (todos) | `docker compose up --build`                            |
| Contenedor falla pero imagen está bien   | `docker compose up --force-recreate`                   |
| Parar todos los servicios                | `docker compose down`                                  |
| Parar todo y borrar volúmenes            | `docker compose down -v`                               |
| Ver logs de un servicio específico       | `docker compose logs -f <servicio>`                    |

---

✨ **Tip:** Mantén esta guía en el repositorio como `README.md` o `DEPLOY.md` para facilitar el onboarding del equipo.

